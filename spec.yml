name: zero2prod
region: nyc1
services:
  - name: zero2prod
    dockerfile_path: DockerfileDO
    source_dir: .
    github:
      branch: main
      deploy_on_push: true
      repo: mblydenburgh/zero2prod
    health_check:
      http_path: /health_check
    http_port: 8007
    instance_count: 1
    instance_size_slug: basic-xxs
    routes:
      - path: /
